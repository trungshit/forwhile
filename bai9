#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    int t=1,x,i;
    double s=0;
        s=0;
        scanf("%d",&x);
        for(i=1;i<=x;i++)
        {
            t*=i;
            s+=1.0/t;
        }
        printf("%llf",s);
     return 0;
}
